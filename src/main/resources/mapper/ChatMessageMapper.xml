<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yy.my_tutor.chat.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.yy.my_tutor.chat.domain.ChatMessage">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="conversation_id" property="conversationId" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="context" property="context" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
    </resultMap>

    <resultMap id="UserMap" type="com.yy.my_tutor.user.domain.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_account" property="userAccount" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="tel" property="tel" jdbcType="VARCHAR"/>
        <result column="country" property="country" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="grade" property="grade" jdbcType="VARCHAR"/>
        <result column="create_at" property="createAt" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_at" property="updateAt" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="VARCHAR"/>
        <result column="role" property="role"  jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, conversation_id, user_id, type, context, create_time, title
    </sql>

    <insert id="insert" parameterType="com.yy.my_tutor.chat.domain.ChatMessage">
        insert into chat_message (
            conversation_id, user_id, type, context, create_time, title
        ) values (
            #{conversationId}, #{userId}, #{type}, #{context}, NOW(), #{title}
        )
    </insert>

    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from chat_message
        where id = #{id}
    </select>

    <select id="findByConversationId" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from chat_message
        where conversation_id = #{conversationId}
        order by create_time asc
    </select>

    <select id="findByUserId" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from chat_message
        where user_id = #{userId}
        order by create_time DESC
    </select>

    <update id="update" parameterType="com.yy.my_tutor.chat.domain.ChatMessage">
        update chat_message
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="context != null">context = #{context},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="title != null">title = #{title},</if>
        </set>
        where conversation_id = #{conversationId}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from chat_message where id = #{id}
    </delete>

    <delete id="deleteByConversationId" parameterType="java.lang.String">
        delete from chat_message where conversation_id = #{conversationId}
    </delete>

    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from chat_message
        order by create_time desc
    </select>

    <select id="findUserByParent" resultType="com.yy.my_tutor.user.domain.User">
        SELECT id, user_account, username, sex, age, password, tel, country, email, grade,
               create_at, create_by, update_at, update_by, delete_flag
        FROM user
        WHERE parent_id = #{id}
    </select>
    <select id="findChatByUsers" resultType="com.yy.my_tutor.chat.domain.ChatMessage">
        select
        t1.id, t1.conversation_id, t1.user_id, t1.type, t1.context, t1.create_time, t1.title,t2.username username
        from chat_message t1 left join user t2 ON t1.user_id = t2.id
        where
            t1.user_id IN
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item.id}
        </foreach>
        order by t1.create_time asc
    </select>
    <select id="findParent" resultType="com.yy.my_tutor.user.domain.User">
        SELECT id, user_account, username, sex, age, password, tel, country, email, grade,
               create_at, create_by, update_at, update_by, delete_flag
        FROM parent
        WHERE id = #{id}
    </select>

</mapper>
