<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yy.my_tutor.user.mapper.ParentMapper">

    <resultMap id="BaseResultMap" type="com.yy.my_tutor.user.domain.Parent">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_account" property="userAccount" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="tel" property="tel" jdbcType="VARCHAR"/>
        <result column="country" property="country" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="grade" property="grade" jdbcType="VARCHAR"/>
        <result column="create_at" property="createAt" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_at" property="updateAt" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_account, username, sex, age, password, tel, country, email, grade,
        create_at, create_by, update_at, update_by, delete_flag
    </sql>

    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from parent
        where id = #{id} and delete_flag = '0'
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from parent
        where delete_flag = '0'
        order by create_at desc
    </select>

    <insert id="insert" parameterType="com.yy.my_tutor.user.domain.Parent">
        insert into parent (
            user_account, username, sex, age, password, tel, country, email, grade,
            create_at, create_by, update_at, update_by, delete_flag
        ) values (
            #{userAccount}, #{username}, #{sex}, #{age}, #{password}, #{tel},
            #{country}, #{email}, #{grade}, #{createAt}, #{createBy}, #{updateAt},
            #{updateBy}, #{deleteFlag}
        )
    </insert>

    <update id="update" parameterType="com.yy.my_tutor.user.domain.Parent">
        update parent
        <set>
            <if test="userAccount != null">user_account = #{userAccount},</if>
            <if test="username != null">username = #{username},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="age != null">age = #{age},</if>
            <if test="password != null">password = #{password},</if>
            <if test="tel != null">tel = #{tel},</if>
            <if test="country != null">country = #{country},</if>
            <if test="email != null">email = #{email},</if>
            <if test="grade != null">grade = #{grade},</if>
            <if test="createAt != null">create_at = #{createAt},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="updateAt != null">update_at = #{updateAt},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="deleteFlag != null">delete_flag = #{deleteFlag},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        update parent set delete_flag = '1' where id = #{id}
    </delete>

    <select id="findByUserAccount" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from parent
        where user_account = #{userAccount} and delete_flag = '0'
    </select>

</mapper> 