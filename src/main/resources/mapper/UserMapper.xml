<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yy.my_tutor.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.yy.my_tutor.user.domain.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_account" property="userAccount" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="tel" property="tel" jdbcType="VARCHAR"/>
        <result column="country" property="country" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="grade" property="grade" jdbcType="VARCHAR"/>
        <result column="school" property="school" jdbcType="VARCHAR"/>
        <result column="create_at" property="createAt" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_at" property="updateAt" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="VARCHAR"/>
        <result column="role" property="role"  jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="TINYINT"/>
        <result column="approval_status" property="approvalStatus" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_account, username, sex, age, password, tel, country, email, grade, school,
        create_at, create_by, update_at, update_by, delete_flag
    </sql>
    <update id="update">
        update user
        <set>
            <if test="userAccount != null">user_account = #{userAccount},</if>
            <if test="username != null">username = #{username},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="age != null">age = #{age},</if>
            <if test="tel != null">tel = #{tel},</if>
            <if test="country != null">country = #{country},</if>
            <if test="email != null">email = #{email},</if>
            <if test="grade != null">grade = #{grade},</if>
            <if test="school != null">school = #{school},</if>
            <if test="updateAt != null">update_at = NOW(),</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </set>
        where id = #{id}
    </update>
    <update id="updateParent">
        update parent
        <set>
            <if test="userAccount != null">user_account = #{userAccount},</if>
            <if test="username != null">username = #{username},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="age != null">age = #{age},</if>
            <if test="tel != null">tel = #{tel},</if>
            <if test="country != null">country = #{country},</if>
            <if test="email != null">email = #{email},</if>
            <if test="grade != null">grade = #{grade},</if>
            <if test="school != null">school = #{school},</if>
            <if test="updateAt != null">update_at = NOW(),</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </set>
        where id = #{id}
    </update>

    <select id="findByUserAccount" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>,"S" AS role, NULL AS type, NULL AS approval_status
        from user
        where (user_account = #{userAccount} OR email = #{userAccount}) and delete_flag = '0'
        UNION
        select
        id, user_account, username, sex, age, password, tel, country, email, grade, school,
        create_at, create_by, update_at, update_by, delete_flag,"P" AS role, type, approval_status
        from parent
        where (user_account = #{userAccount} OR email = #{userAccount}) and delete_flag = '0'
    </select>

    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from user
        where id = #{id} and delete_flag = '0'
    </select>
    <select id="findChild" resultType="com.yy.my_tutor.user.domain.User">
        SELECT id, user_account, username, sex, age, password, tel, country, email, grade, school,
               create_at, create_by, update_at, update_by, delete_flag
        FROM user where parent_id = #{id} AND delete_flag = 0
    </select>
    <select id="findParentById" resultType="com.yy.my_tutor.user.domain.User">
        select
        id, user_account, username, sex, age, password, tel, country, email, grade, school,
        create_at, create_by, update_at, update_by, delete_flag
        from parent
        where id = #{id} and delete_flag = '0'
    </select>

    <insert id="insert" parameterType="com.yy.my_tutor.user.domain.User">
        insert into user (
            user_account, username, sex, age, password, tel, country, email, grade, school,
            create_at, create_by, update_at, update_by, delete_flag, parent_id
        ) values (
            #{userAccount}, #{username}, #{sex}, #{age}, #{password}, #{tel},
            #{country}, #{email}, #{grade}, #{school}, #{createAt}, #{createBy}, #{updateAt},
            #{updateBy}, #{deleteFlag}, #{parentId}
        )
    </insert>

    <select id="findStudentsByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>,"S" AS role
        from user
        where delete_flag = '0'
        <if test="name != null and name != ''">
            and (username like concat('%', #{name}, '%') or user_account like concat('%', #{name}, '%'))
        </if>
        order by create_at desc
    </select>
</mapper>
